import 'package:flutter/material.dart';

class RaporlarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Raporlar Sayfası', style: TextStyle(fontSize: 24)),
    );
  }
}